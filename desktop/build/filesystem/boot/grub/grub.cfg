set timeout=5
set default=0

menuentry "Ubuntu MikaOS" {
    set root=(hd0,1)           # Adjust based on your partition layout
    linux /boot/vmlinuz-linux  # Path to the Linux kernel
    initrd /boot/initramfs-linux.img  # Path to the initial RAM disk
}